import {Alert} from "@mui/material";

function CheckoutError() {
    return (
        <Alert variant="filled" severity="error">Oh snap! Change a few things up and ty submitting again.</Alert>
    )
}

export default CheckoutError;